# Тестовый документ, расшаренный с service account (Editor).
# ВАЖНО: тесты ПЕРЕЗАПИСЫВАЮТ содержимое!
TEST_DOC_ID=""

# Папка на Google Drive, расшаренная с service account (Editor).
# Нужна только для test-drive.sh.
TEST_PARENT_FOLDER_ID=""
